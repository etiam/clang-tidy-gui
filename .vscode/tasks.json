{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang-tidy-gui",
            "type": "shell",
            "args": [
                "${file}",
                "${HOME}/workspace/look/build"
            ],
            "command": "python3",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
